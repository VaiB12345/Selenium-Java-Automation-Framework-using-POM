# Selenium-Java-Automation-Framework-using-POM
I created a Base class for browser initialization, separate Page classes for UI elements and actions, and Test classes only for test logic. This structure improves reusability, readability, and maintenance. I execute tests using TestNG and manage dependencies through Maven.
